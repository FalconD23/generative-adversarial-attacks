name: GenPatchTST

params:
  seq_len: ${dataset.seq_len}
  c_in: ${dataset.c_in}

hyperparameters_vary:
  dropout:
    optuna_type: float
    low: 0.1
    high: 0.7
    step: 0.1

attack_train_mode: True
